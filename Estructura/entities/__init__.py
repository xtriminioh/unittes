"""
    Se crea una carpeta, y dentro de la carpeta.
    Para que la carpeta sea tratada como un modulo en python,
    esta tiene que tener un archivo __init__.py dentro.
"""

#exponemos la siguientes clases.
from entities.product import Product
from entities.shopping_cart import ShoppingCart